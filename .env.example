# NextAuth Configuration
NEXTAUTH_URL=https://trickest.vercel.app/
NEXTAUTH_SECRET=[GENERATE_WITH_openssl_rand_-base64_32]

# Debug Mode (ONLY set to 'true' in development for debugging auth)
# NEVER set this in production
DEBUG_AUTH=false

# Logging Level
# Options: trace, debug, info, warn, error, silent
# Development: debug (shows all logs)
# Production: info (hides debug logs)
LOG_LEVEL=info

# Supabase Database Configuration
# DATABASE_URL: connection pooling (pgbouncer) - used by application
# DIRECT_URL: direct connection - used by Prisma migrations
DATABASE_URL=postgresql://[USER]:[PASSWORD]@[HOST]:6543/postgres?pgbouncer=true
DIRECT_URL=postgresql://[USER]:[PASSWORD]@[HOST]:5432/postgres

# Supabase Public Configuration
NEXT_PUBLIC_SUPABASE_ANON_KEY=[YOUR_ANON_KEY]
NEXT_PUBLIC_SUPABASE_URL=[YOUR_SUPABASE_URL]

# Supabase Service Role Key (server-side only, bypasses RLS)
# Get from: Supabase Dashboard > Project Settings > API > service_role (secret)
# IMPORTANT: Never expose this key to client-side code
SUPABASE_SERVICE_ROLE_KEY=[YOUR_SERVICE_ROLE_KEY]

# Google OAuth Configuration
GOOGLE_CLIENT_ID=[YOUR_GOOGLE_CLIENT_ID]
GOOGLE_CLIENT_SECRET=[YOUR_GOOGLE_CLIENT_SECRET]

# Backend Configuration
NEXT_PUBLIC_BACKEND_URL=https://skaters.toryskateshop.com

# Cron Jobs (FASE 2 - Sistema de Votación)
# Secret token para proteger endpoints de cron jobs
# Generar con: openssl rand -base64 32
CRON_SECRET=[GENERATE_WITH_openssl_rand_-base64_32]
